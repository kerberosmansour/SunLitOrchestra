# `docs/slo/current/` — runbooks in progress

Drop a runbook here when its first milestone flips from `not_started` to `in_progress`. Move it to [`../completed/`](../completed/) once the final milestone is `done`. Newly authored runbooks (output of `/slo-plan`) land here by default.
