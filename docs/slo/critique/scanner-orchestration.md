# Critique — scanner-orchestration

> Generated by `/slo-critique scanner-orchestration` on 2026-04-26.
> Reviews [docs/slo/completed/RUNBOOK-SCANNER-ORCHESTRATION.md](../completed/RUNBOOK-SCANNER-ORCHESTRATION.md) using the CEO / Eng-lead / Security / Design rotation.
> Auto-fix findings have been applied inline. `ask` findings need user acceptance before any milestone starts. `defer` findings are recorded for v2 / future runbooks.

## Persona summary

| Persona | Findings | Auto-fix | Ask | Defer | Notes |
|---|---|---|---|---|---|
| CEO | 1 | 0 | 1 | 0 | Wedge-validation timing |
| Eng-lead | 8 | 1 | 6 | 1 | Surface-fragility, conflict, rate-limit, licensing, isolation, citations, brittleness, contract-evolution |
| Security | 5 | 0 | 4 | 1 | Symlink, YAML deser, variant-extend, argv-list git, no-`--repo` flag |
| Design | 1 | 0 | 0 | 1 | Preview-mode CI stdin block (v2) — runbook has no UI surface, single finding noted then skipped |
| **Total** | **15** | **1** | **11** | **3** | |

## Findings

### Format key

- **id** — stable identifier per persona (CEO-N, ENG-N, SEC-N, DSGN-N).
- **category** — `auto-fix` (applied inline); `ask` (user must accept); `defer` (v2 / future runbook); `reduce-scope` (CEO only); `hold-scope` (CEO only).
- **runbook section** — exact section reference for the executor.
- **concrete scenario** — actor + action + bad outcome (mandatory; no vague findings).

---

### CEO pass — scope challenge

| id | persona | category | runbook section | finding | concrete scenario | recommendation |
|----|---------|----------|-----------------|---------|-------------------|----------------|
| **CEO-1** | CEO | ask | Background Context — Key Design Principles + M2 step-by-step | Wedge thesis is validated only at M5 (dogfood). If the threat-model parse contract is too restrictive in practice (e.g., users want richer CWE expression, or the scope rule excludes content they wanted included), the architecture is locked in by then and reversal is expensive. | Team builds M1 + M2 + M3 over 3 weeks. At M4 preview-mode lands and the user finds the threat-model file too verbose to maintain — they want a simpler input format (e.g., `cwes: [77, 78, 89]` frontmatter rather than prose mentions). Architecture is locked; backing out requires re-running `/slo-architect` and shedding 4 milestones of work. | Add a "wedge validation" step to M2's smoke tests: invoke the parser against 3 real OSS threat models from public repos (or 3 contrived examples representing solo-OSS-maintainer style) before the implementer commits to M3. Document acceptance criteria: parser produces meaningful CWE lists on all 3 fixtures with zero false-positives from the scope rule. If validation fails, halt the runbook and re-do `/slo-ideate` Q3 (the wedge question). |

---

### Eng-lead pass — architecture pokes

| id | persona | category | runbook section | finding | concrete scenario | recommendation |
|----|---------|----------|-----------------|---------|-------------------|----------------|
| **ENG-1** | eng | ask | M3 — BDD scenarios + Smoke Tests | The "static workflow template" passes the structural-contract test by parsing emitted YAML — but doesn't validate that Semgrep accepts the workflow at runtime. If Semgrep deprecates `SEMGREP_RULES` (research synthesis flagged the `--config` vs `SEMGREP_RULES` ambiguity as an open question) or changes its CLI surface, the structural test stays green while real users get broken workflows. | Semgrep 1.180 deprecates `SEMGREP_RULES`; new users invoking `/slo-sast` get a workflow that prints deprecation warnings, eventually breaks; the structural-contract test still passes. CI for the SLO repo doesn't catch it because no real Semgrep is invoked against an emitted workflow. | Add an M3 BDD `workflow_runs_against_real_semgrep_dry_run` (or smoke test) that pipes the emitted workflow through `semgrep ci --dry-run --no-rewrite-rule-ids` against a fixture target repo. Treats Semgrep CLI as integration boundary; catches deprecations / CLI-surface changes. |
| **ENG-2** | eng | ask | M2 — BDD scenario `cache_hit_skips_clone` | The BDD says "git clone is NOT invoked" on cache hit — but a defense-in-depth `git rev-parse HEAD` post-cache-hit (to verify integrity) would also be a `git` invocation and would fail this assertion. Risk: implementer adds a sensible integrity check, BDD breaks, integrity check is removed. | M2 implementer adds `git rev-parse HEAD` on cache-hit to defend `tm-scanner-orchestration-abuse-2` (a maliciously pre-populated cache where the directory name says SHA A but the actual commit content is SHA B). The cache_hit_skips_clone BDD fails because rev-parse counted as a git invocation. The integrity check is reverted to make the test pass; the defense is lost. | Tighten the BDD assertion: "no `git clone` invocation" (specifically the `clone` subcommand), not "no `git` invocation at all". Allow `rev-parse`, `cat-file`, etc. for integrity verification. Document this in `references/sast/scanner-orch-pinned-rules-sha.md`. |
| **ENG-3** | eng | ask | M3 — Out of Scope + M4 preview-mode | Emission unconditionally writes `.github/workflows/sast.yml` into the target repo. If the target repo already has a workflow at that path (from a prior tool, manual setup, or a different version of `/slo-sast`), it gets overwritten without warning. Preview-mode (M4) gates only on `manifest.json` absence — a pre-existing `sast.yml` without a manifest is silently clobbered. | Solo OSS maintainer has hand-authored `.github/workflows/sast.yml` running CodeQL with custom queries. Runs `/slo-sast` to evaluate it. Skill overwrites the CodeQL workflow with a Semgrep workflow. Maintainer's CodeQL config is gone; they must dig through git history to restore it. | Extend M4 preview-mode trigger: detect pre-existing `.github/workflows/sast.yml` (any contents) AND/OR pre-existing `.semgrep.yml`. Either condition triggers preview, even when `.semgrep/manifest.json` is absent. Surface the diff in preview output. |
| **ENG-4** | eng | ask | M5 — Contract Block "Forbidden shortcuts" + rate-limit cap | "5 PRs/hour per session" defined as in-process counter. But `/slo-sast` invocations are typically separate processes (each `claude /slo-sast …` invocation spawns fresh). In-process counter resets per invocation; cap is bypassed by simply re-invoking. | Re-derivation loop fires (some bug or misconfigured threat-model auto-edit); user/script invokes `/slo-sast` 6 times in succession (each a separate process); each gets fresh counter; rate limit silently bypassed; 6 PRs flood the user's repo. | Either (a) persist the counter to `~/.cache/sldo/scanner-orch-rate-limit-state.json` with file-locked atomic writes, OR (b) redefine the cap as "max 1 PR per invocation" (the natural single-invocation limit) and rely on external mechanisms (CI throttling, threat-model edit cadence) for the cross-invocation rate. Choice should be made before M5 starts; document in `references/sast/scanner-orch-rederivation-triggers.md`. |
| **ENG-5** | eng | ask | M2 — fixture creation step + Definition of Done | Test fixtures under `crates/sldo-install/tests/fixtures/scanner-orch/m2/cache/` reference real Semgrep rule files (per the BDD "concrete proof" research finding using `python.django.security.injection.sql.sql-injection-using-raw.yaml`). Semgrep community rules ship under the Semgrep Rules License, which has notice / attribution requirements. SLO is dual-licensed Apache-2.0 OR MIT; no Semgrep license notice currently in repo. | M2 lands with copied rule fixtures from `semgrep-rules`. A future SLO release distribution includes these rule bytes. Semgrep contributors notice the redistribution lacks the required attribution; file an issue / DMCA. SLO has a licensing breach to fix retroactively. | Before M2 fixture-authoring step, verify Semgrep Rules License terms. If notice is required, either (a) add a `NOTICE` file under `crates/sldo-install/tests/fixtures/scanner-orch/m2/` with the license text, OR (b) use **synthetic** rule YAML fixtures (handcrafted to match the metadata schema, not copied byte-for-byte from upstream). Option (b) is safer and still validates the parsing contract. |
| **ENG-6** | eng | ask | M5 — Smoke Tests + Step-by-Step | The "isolated SLO subtree" for the dogfood test is hand-waved. If implemented via symlinks into the real `docs/slo/design/scanner-orchestration-*.md`, a rogue test command could mutate the real repo. | Dogfood test uses symlinks to the real `docs/` files; a bug in the skill (or a future test extension) issues a `Write` operation through the symlink; the real `docs/slo/design/scanner-orchestration-threat-model.md` is mutated; commit hash diverges; subsequent runs fail mysteriously. | Mandate file-content **copy** (not symlinks) for the dogfood subtree fixture. Use `tempfile::TempDir` + recursive copy of the relevant subtree before each test. Document the isolation discipline in M5's step-by-step. |
| **ENG-7** | eng | **auto-fix (applied)** | M1 — Contract Block, Data classification cell | M1's data-classification cell cited `references/proactive-controls-vocabulary.md` which doesn't exist in the repo (that path is in `/slo-plan`'s SKILL.md but is a missing artifact in this codebase). Citation creates a dead link in the runbook. | An executor reading M1 follows the citation; finds no file at `references/proactive-controls-vocabulary.md`; either creates a confused "where is this?" question OR fabricates a stub. | **APPLIED**: removed the parenthetical `Per [references/proactive-controls-vocabulary.md](...) (cited by /slo-plan; if absent, OWASP Proactive Controls v3 categories are used directly).` from M1's data-classification cell. The substantive content (Internal, with reasoning) stands without the dead-link citation. M2-M5 already use OWASP C-numbers directly without citing the missing file. |
| **ENG-8** | eng | ask | M2 — Outputs row + M1 E2E tests | M1 E2E tests assert M1's stdout shape as a flat list-of-strings (e.g., `["CWE-77", "CWE-78", "CWE-89"]`). M2 changes the SKILL's output to a JSON object envelope. The M1 E2E tests will fail at M2 landing; engineer "fixes" by editing M1's test assertions, losing M1's explicit contract validation. | M2 lands. M1 E2E tests fail because stdout is now `{"cwes_extracted": [...], ...}` instead of a bare list. Engineer updates `e2e_scanner_orch_m1.rs` to look for `cwes_extracted` key — but in doing so, deletes the assertion that the parser scope rule produced exactly the expected list (because the test was written against the M1 stdout shape). M1's structural contract is now untested. | Make the M1→M2 contract evolution explicit: M2's compatibility commitments add an explicit step "rewrite M1 E2E tests to assert against the `cwes_extracted` field of the JSON envelope, preserving every M1 scope-rule assertion" + name the M1 tests that need migration. Better — refactor M1 E2E tests on the M2 branch as part of the M2 work, with explicit migration commits, not silent edits. |

---

### Security pass — class elimination + variant analysis + threat-model citation

| id | persona | category | runbook section | finding | concrete scenario | recommendation |
|----|---------|----------|-----------------|---------|-------------------|----------------|
| **SEC-1** | security | ask | M3 — emission step + threat-model.md STRIDE row "`.semgrep/rules/`" | **Bug class**: path traversal via symlink-following on file emission. **Threat-model row**: extends the existing M3 emission STRIDE row (currently asserts `eliminated by git` for tampering — but the row covers post-emission tampering, not the emission moment itself). **Class status**: NOT eliminated by current design — the threat model doesn't address symlink-following at the write site. **Variant analysis**: applies to `.semgrep/rules/` (M3), `.github/workflows/sast.yml` (M3), `.semgrep.yml` (M3), `.semgrep/manifest.json` (M4), `.semgrep/last-run.json` (M4). | An attacker submits a malicious PR (or a dependabot-style automated PR) that changes `.semgrep/rules/` from a directory to a symlink pointing at `/etc/cron.d/`. PR is accepted (the change is subtle). Next `/slo-sast` invocation writes `<rule-id>.yaml` files to `/etc/cron.d/` running as the user's UID. Cron picks them up as cron files, executes whatever's there. RCE on the developer's machine. | Add to M3 step-by-step: before any write into `.semgrep/`, the skill MUST check that each path component is a directory (not a symlink); use `O_NOFOLLOW`-style file creation for atomic writes. Add M3 BDD `refuses_emission_when_dot_semgrep_is_symlink`. Variant: same for `.github/workflows/`. The variant-analysis playbook would suggest grepping the SKILL.md for every `Write` operation and asserting symlink-resistance per write. |
| **SEC-2** | security | ask | M2 — BDD scenario set | **Bug class**: deserialization attack via attacker-controlled YAML input (billion-laughs, alias bombs, large-string DoS). **Threat-model row**: `tm-scanner-orchestration-abuse-2` (compromised semgrep-rules upstream) + STRIDE for `~/.cache/sldo/semgrep-rules/<SHA>/`. **Class status**: mitigated by SHA pin (the malicious commit can't sneak in without a pin bump that surfaces as a diff PR), BUT not eliminated — once a malicious SHA is pinned, parsing is unbounded. **Variant analysis**: same applies to any tool consuming `.semgrep.yml` (the user's `semgrep ci` invocation in CI), so this is partially "downstream's problem" — but the skill's own parser (used by the test harness or by the skill's filter step) is also vulnerable. | A compromised maintainer of `semgrep-rules` pushes a rule YAML containing a billion-laughs entity expansion (`a: &a "..." \n b: *a *a *a ...`). User accepts a routine SHA-bump PR (the diff looks like a normal rule update because the malicious YAML is buried in one rule file). Next `/slo-sast` invocation parses the rule; YAML expansion blows up to gigabytes; OOM kill on the developer's machine. Less severe variant: a 100MB inline string in `metadata.description` causes the skill to read it into memory, eating RAM. | Verify `serde_yaml_ng` defaults are safe (no entity expansion, no anchor recursion); assert via M2 BDD `refuses_yaml_with_billion_laughs` against a synthetic fixture (handcrafted YAML, not copied — also addresses ENG-5's licensing concern). Add memory cap to YAML parsing (e.g., reject any single rule YAML over 1 MiB before parse). Document in `references/sast/scanner-orch-pinned-rules-sha.md` that pin-bump diffs MUST be reviewed for unusual content size. |
| **SEC-4** | security | ask | M3 — BDD scenarios | **Bug class**: prompt injection / template injection. **Threat-model row**: `tm-scanner-orchestration-abuse-3`. **Class status**: eliminated by M3's "static workflow template" design — but the existing BDD `cwe_list_changes_do_not_change_workflow_yaml` only asserts the `sast.yml` is byte-static across CWE-disjoint threat models. It does NOT assert the same for `.semgrep.yml`. **Variant analysis**: extend the test to ALL emitted artifacts whose YAML structure should be CWE-independent. | An attacker crafts a threat model with a CWE description containing a YAML breakout sequence. M3's `sast.yml` is correctly resistant (asserted by BDD). But `.semgrep.yml` emission is not asserted — a future implementer might add CWE-driven content to `.semgrep.yml` (e.g., comments listing claimed CWEs); the prompt-injection content lands in `.semgrep.yml`; downstream tools consuming `.semgrep.yml` parse the injected content. | Extend M3 BDD `cwe_list_changes_do_not_change_workflow_yaml` to ALSO assert: `.semgrep.yml` is byte-identical across the same CWE-disjoint threat-model fixture pair. Pair this with the structural-contract test asserting both files satisfy a "no user-prose content" property (regex check that `.semgrep.yml` matches a small grammar). |
| **SEC-6** | security | ask | M2 — Forbidden shortcuts row + step-by-step | **Bug class**: command injection via subprocess argument interpolation. **Threat-model row**: not directly covered (extends `tm-scanner-orchestration-abuse-2`). **Class status**: depends on implementation — argv-list invocation eliminates the class; shell-string invocation does not. The runbook says "uses `git` CLI for clone" but doesn't specify argv-list discipline. **Variant analysis**: applies to every subprocess invocation in the skill — `git clone`, `git rev-parse`, `gh pr create`, `semgrep --version` (M4). | An attacker compromises `references/sast/scanner-orch-pinned-rules-sha.md` via a PR that passes review (reviewer skims the SHA value but doesn't notice it's `abc123...; rm -rf $HOME`). Next `/slo-sast` invocation runs `git clone https://github.com/semgrep/semgrep-rules <evil-sha-with-metachars>`. If the skill uses shell interpolation (e.g., string concatenation with `bash -c`), the metacharacters execute. If the skill uses argv-list (e.g., `Command::new("git").arg("clone").arg(url).arg(sha)`), the metacharacters are passed as a literal arg and `git` rejects the input cleanly. | Add to M2 forbidden shortcuts: "no shell-string subprocess invocation; all subprocess calls use argv-list form (in SKILL.md prose: explicit `git`/`gh` call with each argument separated by whitespace or quoted-list syntax that the executor recognizes as separate args, never shell-piped strings)." Also: keep the M2 BDD `refuses_short_sha_prefix` (input validation eliminates this attack at validation step), but add defense-in-depth via argv-list discipline. Apply the same rule to M5's `gh pr create` (M5 already has rate-limit/no-merge-flag rules; add no-shell-interpolation). |
| **SEC-8** | security | ask | M5 — Contract Block "Forbidden shortcuts" + BDD `no_cross_repo_filing` | **Bug class**: confused deputy via `gh` CLI's `--repo` flag interaction with `git remote get-url origin`. **Threat-model row**: extends `tm-scanner-orchestration-abuse-3` to the M5 PR-creation surface. **Class status**: mitigated by `gh`'s default behavior (uses `git remote get-url origin` for `--repo` resolution), but not eliminated — passing `--repo` explicitly could redirect filing. **Variant analysis**: also applies to any future cross-repo filing in subsequent skills (e.g., `/slo-sec-libs`'s issue filing). | An attacker emails the user a "test fixture" tarball that, when extracted, contains a working-tree-shape with `.git/config` pointing at `[remote "origin"] url = https://github.com/anthropic-private/secret-repo`. User runs `/slo-sast` to "demo" the skill inside the tarball directory. The skill detects drift, runs `gh pr create` — if it explicitly passes `--repo anthropic-private/secret-repo` (e.g., from reading `.git/config`), the user's `gh` auth files a PR against a private Anthropic repo. | Add to M5 forbidden shortcuts: "no `--repo` flag in `gh pr create` argv; rely on `gh`'s default origin-based resolution." Extend the M5 BDD `no_cross_repo_filing` to assert specifically that `--repo` is absent from captured argv (currently the BDD asserts the `--repo` value matches, which is weaker — explicit passing of `--repo` should be forbidden entirely). Belt-and-suspenders. |
| **SEC-5** | security | defer | scanner-orchestration-threat-model.md residual risks | **Bug class**: resource exhaustion via uncapped storage growth. **Threat-model row**: residual risk #3 (cache pruning unmanaged). **Class status**: residual — already documented in v1 as accepted, no defense planned for v1. **Variant analysis**: applies to any future caching the skill pack adds. | A shared CI runner is used by 50 developers, all running `/slo-sast` against various target repos. Each developer's `~/.cache/sldo/semgrep-rules/` accumulates SHA snapshots over time (one per pin bump, ~6 months of pins = ~12 snapshots × ~50 MB each = ~600 MB per developer). Combined: ~30 GB. Runner exceeds disk quota; CI fails; ops team scrambles to identify cache-eaters. | Defer to a Phase-2 hardening runbook. v1 acknowledgement is acceptable. Add a single-line warning in `references/sast/scanner-orch-pinned-rules-sha.md` reminding bumpers that old caches accumulate and pruning is the user's responsibility. |

---

### Design pass — UI / interaction surface

The runbook has no UI surface — `/slo-sast` is a CLI-style skill emitting stdout + filesystem artifacts. Design pass skipped per the skill's "Skip with a note if N/A" rule. One interaction-surface finding deferred for completeness:

| id | persona | category | runbook section | finding | concrete scenario | recommendation |
|----|---------|----------|-----------------|---------|-------------------|----------------|
| **DSGN-1** | design | defer | M4 — Preview-Mode UX | Preview-mode blocks on stdin for user confirm (`y`/`n`). In an automated CI context (where `/slo-sast` might be invoked headlessly via Claude Code's batch mode), stdin-blocking would hang indefinitely. v1 ships interactive-only. | A future user wires `/slo-sast` into a CI matrix job that re-derives weekly. The headless invocation prompts on stdin; CI never responds; CI hangs until job timeout (typically 6 hours). User wastes runner-minutes and gets confused timeout failures. | v2 concern: add a `--confirm` (or `--non-interactive`) flag that pre-accepts the preview, AND/OR detect non-interactive context (`isatty` check on stdin) and exit non-zero with a clear stderr message naming the missing flag. Document in v2 runbook. |

---

## Auto-fix changelog

| Finding | Action |
|---|---|
| ENG-7 | Removed dead-link citation from M1's data-classification cell. The substantive content remains; M2-M5 cells were already citation-clean. |

---

## What the user should do next

1. **Review the 11 `ask` findings.** Each is a real concern with a concrete scenario and an actionable recommendation. Accept, decline, or modify each:
   - CEO-1 — wedge validation timing (add to M2 smoke?)
   - ENG-1 — real-Semgrep dry-run smoke in M3 (add it?)
   - ENG-2 — tighten M2 BDD to "no `git clone`" specifically (apply?)
   - ENG-3 — preview-mode trigger on pre-existing workflow (extend M4?)
   - ENG-4 — rate-limit definition: persisted counter or per-invocation cap (decide which)
   - ENG-5 — Semgrep license review for M2 fixtures (verify or use synthetic)
   - ENG-6 — copy-not-symlink for dogfood subtree (mandate?)
   - ENG-8 — explicit M1→M2 test migration in M2's compatibility commitments (add?)
   - SEC-1 — symlink-traversal defense at every emit site (add to M3 + M4?)
   - SEC-2 — billion-laughs YAML defense in M2 (add BDD?)
   - SEC-4 — extend M3 byte-compare to `.semgrep.yml` (extend?)
   - SEC-6 — argv-list subprocess discipline in forbidden shortcuts (add to M2 + M5?)
   - SEC-8 — explicit no-`--repo` flag in M5 `gh pr create` (tighten?)
2. **Confirm the 3 `defer` findings** are appropriately deferred (SEC-5 cache exhaustion, DSGN-1 CI stdin block).
3. **The 1 `auto-fix` (ENG-7) is already applied** — verify by reviewing M1's data-classification cell in the runbook.
4. **Once asks are accepted/declined, kick off `/slo-execute M1`** to start implementation.

The runbook is ready for execution **after** the asks are resolved. Skipping the asks is allowed (they are findings, not gates), but each one names a concrete failure mode the user would likely regret after shipping.
