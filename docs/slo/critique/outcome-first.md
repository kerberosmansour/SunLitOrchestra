# Critique — RUNBOOK-outcome-first.md

> Four-persona adversarial review (CEO, Eng lead, Security, Design) of `docs/RUNBOOK-outcome-first.md` (Outcome First Engineering, 5 milestones).
> Threat model read from the frozen `docs/slo/design/outcome-first-threat-model.slo.json` (abuse cases `tm-outcome-first-abuse-1/2/3`; residuals R1/R2/R3 are `accepted_residual: true` and are NOT re-flagged here).
> Reviewed 2026-06-17.

## Findings

| id | persona | category | runbook section | finding | concrete scenario | recommendation |
|----|---------|----------|-----------------|---------|-------------------|----------------|
| CEO-1 | CEO | ask | §5A Measurement Contract / Rollout | The value thesis ("does the gate catch real misses?") isn't tested until **after M5** — feedback is late and a latent Pass 0 design flaw would surface only after 5 milestones are sunk. | M1–M2 ship the template + plan-enforcement with zero observable "the gate caught something"; the first real proof is a Pass 0 block, which §5A schedules "on the next real feature runbook after M5." If Pass 0's front-to-end design is wrong (see ENG-2/SEC-2), the cost is discovered last. | Add a **dogfood checkpoint after M3**: run Pass 0 on one real value-bearing milestone (e.g. this runbook's own M4/M5) with a deliberately-unproven outcome row and confirm it blocks, *before* committing M4/M5. Update §5A Review-windows/Rollout. |
| CEO-2 | CEO | hold-scope | §2 Milestone Tracker | The "aha" (a milestone blocked because the outcome is absent) lives in M3–M4, behind two scaffolding milestones (M1 template, M2 plan). | You cannot gate on §5C/§17 sections that don't exist yet, so template→enforcement→gate→close is a forced dependency order, not a buried lede. | No re-order. Informational — pair with CEO-1 so the aha is *demonstrated* mid-stream rather than only after M5. |
| ENG-1 | Eng lead | auto-fix ✅ applied | design docs (overview/interfaces/reversibility/code-map) | The design docs still said "**Pass 1** = Outcome Validation, **demote**/re-index Passes 1–3", contradicting the confirmed M3 decision "**Pass 0**, non-renumbering". | An executor reading the interfaces §Milestone-shape-hint M3 would renumber Pass 4/5/6 — breaking exactly the repo-wide citations the Pass 0 choice exists to protect (the D5 seam). | **Fixed inline**: reconciled `outcome-first-overview.md`, `-interfaces.md`, `-reversibility.md` (D5), `-code-map.md` to "Pass 0, non-renumbering." No runbook change needed (M3 was already correct). |
| ENG-2 | Eng lead | ask | M1 §5C Front-to-End Validation | §5C's ordered Front-to-End steps (seed→backend→persisted→API/IPC→UI) are a **monolithic block**; for a no-UI / library / CLI target (including *this* repo) the UI and sometimes API/IPC layers are genuinely N/A. | A milestone adding a pure Rust library fn has no UI layer; the author either fakes a UI step (theatre) or marks the whole §5C row `N/A` — losing the backend→persisted proof that *does* apply. | Make the Front-to-End steps **per-layer** `applicable \| not_applicable(reason)`, and require **≥1 real cross-layer assertion** (e.g. backend→persisted) for any value-bearing milestone, so the row can never degrade to nothing. Bake into M1's §5C design + the M1 test. |
| ENG-3 | Eng lead | ask | §5A + the M1–M5 `outcome_first_*` tests | The structural tests prove the contract is **documented** (sections/markers/SHA present) — not that Pass 0 **fires** on a real milestone. The "gate works" claim rests on agent compliance with prose. | Every `outcome_first_m*_*.rs` can be green while a real `/slo-verify` run skips Pass 0 entirely; nothing in the runbook executes a real outcome journey end-to-end. | Make the CEO-1 dogfood an **explicit acceptance step**: on one real milestone, force an unproven outcome row and confirm `/slo-retro` refuses + Pass 0 reports fail. Record it as the runbook's lagging-metric proof (ties CEO-1). |
| ENG-4 | Eng lead | ask | M4 (SHA lockstep) | `slo-critique` SHA is pinned in **two** tests; M4 bumps both constants by hand. A half-update fails only in the *old* test with a cryptic "F-ENG-6 lockstep broken." | Executor bumps the `sap_imp_m5_agents.rs` constant, forgets `slo_tm_m2_consumers.rs` → confusing red in an unrelated-looking test, hard to diagnose mid-milestone. | In `outcome_first_m4_consumers.rs`, assert **both** critique baselines equal the live `slo-critique/SKILL.md` SHA (one cross-check) so a half-update fails loudly in the new, named test. |
| SEC-1 | Security | ask | M4 allow-list | **New surface the threat model didn't see** (V17 — weakened security control): M4 allow-lists two existing SHA-pin test files. Risk: the executor *weakens/removes* the F-ENG-6 assertion to get green instead of updating only the constant — silently dropping the canonical-critique-path tamper-evidence. Class **V17**; threat-model: no covering row (gap vs `/slo-architect`). | A frustrated executor comments out the SHA assert in `slo_tm_m2_consumers.rs` to pass; `slo-critique/SKILL.md` can thereafter be modified undetected — the exact integrity control F-ENG-6 exists to provide is gone. | (a) M4 contract: in those two files **only the baseline constant value may change**; assertion logic must be byte-stable otherwise (add to Forbidden shortcuts + a guard). (b) Add **`tm-outcome-first-abuse-4`** to the threat model (`.md` + `.slo.json`) for this surface. Variant analysis: 2 pin sites — `rg -n "F-ENG-6" xtasks/sast-verify/tests/` → `sap_imp_m5_agents.rs`, `slo_tm_m2_consumers.rs`, both in M4's allow-list. |
| SEC-2 | Security | ask | M1 §5C / M3 Pass 0 | abuse-2 (outcome-test **theatre**) mitigation degrades on no-UI/library targets: when Front-to-End collapses to `N/A` (ENG-2), Pass 0's "runs front-to-end (Playwright for UI)" defense evaporates and a mock-only assertion passes. Class **V11** (workflow skip) — residual of `tm-outcome-first-abuse-2`; status mitigated-but-residual-for-non-UI. | A library-only milestone marks UI `N/A` and asserts a mocked return; Pass 0 has no UI to drive, so theatre survives the gate the whole change exists to prevent. | Adopt ENG-2's ≥1-real-cross-layer-assertion rule as the theatre control for non-UI targets; state in M3's Pass 0 that "front-to-end" means the highest *applicable* layer chain, never a single mock. Variant analysis: N/A — single-surface design property, not a code pattern. |
| DESIGN | Designer | N/A | — | No UI surface (Markdown skills + Markdown template + one Rust structural test). | — | Skipped per persona rule. |

## Threat-model honesty check (read from `.slo.json`)

- `tm-outcome-first-abuse-1/2/3` are all cited by the runbook's per-milestone abuse-acceptance rows (M1 / M2-M3-M4 / M4 respectively). ✅
- Residuals R1/R2/R3 carry `accepted_residual: true` — **not** re-flagged as missing coverage (per the read-side contract). ✅
- **One gap**: SEC-1 identifies a surface (editing the SHA-pin tests) the threat model didn't model → recommend adding `tm-outcome-first-abuse-4` (a finding against the threat model, surfaced here, applied in M4 if accepted).

## Disposition (resolved 2026-06-17)

- **Auto-fix applied**: ENG-1 — design-doc Pass-0 reconciliation done inline across `overview`, `interfaces`, `reversibility`, `code-map`, `threat-model.md`, `threat-model.slo.json`, `stack-decision` (every "Pass 1 = Outcome Validation / demote / re-index" → "Pass 0, non-renumbering").
- **Hold-scope (informational)**: CEO-2 (forced ordering is correct — no change).
- **Asks — ALL THREE THEMES ACCEPTED by the founder and folded into the runbook**:
  - **Theme A (CEO-1 + ENG-3)** — §5A Review-windows/Rollout + M3 Post-Flight + M4 Notes now carry a mid-stream dogfood acceptance checkpoint after M3 (a real Pass 0 blocks a real milestone before M4/M5).
  - **Theme B (ENG-2 + SEC-2)** — M1 §5C + M2 reference + M3 Pass 0 now require per-layer `applicable | not_applicable(reason)` Front-to-End steps with ≥1 real cross-layer assertion; abuse-2 control updated in the threat model.
  - **Theme C (ENG-4 + SEC-1)** — M4 now cross-checks both `slo-critique` baselines == live SHA, forbids weakening the pin-test assertions (constant-only), and `tm-outcome-first-abuse-4` was added to the threat model (`.md` + `.slo.json`, contiguous 1–4).

No `severity: high|critical` findings → no expanded security appendix required. All `ask`s resolved → `/slo-execute M1` is unblocked.
