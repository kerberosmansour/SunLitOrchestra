# Critique — slo-threat-model-producer runbook

Target: [docs/RUNBOOK-SLO-THREAT-MODEL-PRODUCER.md](../../RUNBOOK-SLO-THREAT-MODEL-PRODUCER.md)
Threat model: merged [docs/slo/design/slo-threat-model-threat-model.md](../design/slo-threat-model-threat-model.md) (same feature family; no `slo-threat-model-producer-threat-model.slo.json` exists → read-side contract **degraded mode**: proceed, no abuse-ID-stability claim, do not block).
Prior lessons applied: `slo-threat-model-m1.md` / `-m2.md` ("read the file before asserting"; "a guard must bind the invariant, not the prose"; "rg every test that reads a file you edit").
Date: 2026-05-19. Personas: CEO → Eng → Security → Design.

| id | persona | category | runbook section | finding | concrete scenario | recommendation |
|----|---------|----------|-----------------|---------|-------------------|----------------|
| CEO-1 | CEO | hold-scope | §5–§6 | The loop "closes" only structurally — the producer is prose `/slo-architect` follows; no local harness runs a real agent emission. This is the whole skill-pack's nature (true of the merged wedge too), not a producer-specific defect. 2 milestones, SEC-2/OTM correctly excluded. | A user runs `/slo-architect` on a new feature post-merge; whether the agent actually emits a conforming `.slo.json` is unverifiable in CI — only that the contract instructs it to. | No change. Recorded so it is conscious, not silent. The user would not alter the plan for an accepted property of a prompt-based pack. |
| ENG-1 | Eng lead | ask | M1 — BDD `idempotent_reemit_preserves_ids`, invariant (c), DoD | The scenario claims the test proves "idempotent re-emit preserves ids", but **nothing re-emits** — the producer is prose, no code runs it. `slo_tmp_m1_producer` re-parsing the *committed* fixture and asserting its ids are unchanged is comparing the file to itself (tautological); it cannot verify the documented *process* preserves ids. Over-claiming guard — the exact prior-lesson anti-pattern ("bind the invariant, not the prose around it"). | Execution writes a test that "passes", a reviewer reads `idempotent_reemit_preserves_ids` as proof the producer is idempotent, ships it; a later real `/slo-architect` re-run renumbers ids and nothing caught it because the test never exercised re-emission. | Reframe to a **checkable structural proxy**: assert (a) the threat-model-template.md serialization mapping maps the Markdown `tm-<slug>-abuse-N` column 1:1 onto the JSON `id` field and explicitly forbids renumber, AND (b) the merged dogfood fixture still conforms with ids `tm-slo-sec-abuse-1..8`. Drop the word "proves idempotent re-emit"; state the honest structural claim in the BDD/DoD/invariant. |
| ENG-2 | Eng lead | ask | M2 — BDD `live_superseded_row_validates`, Compatibility, Evidence "Schema regression on new fixture", regression list | **Factual error.** `xtasks/sast-verify/tests/slo_tm_m1_schema.rs` loads ONE hardcoded `const FIXTURE = "docs/slo/design/slo-security-embedding-threat-model.slo.json"` — it does not glob. The M2 runbook repeatedly claims the merged `slo_tm_m1_schema` "passes against BOTH the original and the new superseded fixture"; it will never even read the new demo fixture. | Execution trusts the runbook, relies on `slo_tm_m1_schema` to validate the new superseded fixture, writes a thin `slo_tmp_m2_rerun`, and the demo fixture's schema-conformance is in fact unverified — a broken superseded row could ship. | `slo_tmp_m2_rerun` MUST itself strict-parse the demo fixture with the same serde structs + frozen-ID/superseded invariants (smallest fix), OR refactor `slo_tm_m1_schema` to a shared helper parameterised over a fixture list. Correct every M2 row that claims `slo_tm_m1_schema` validates the new fixture. |
| SEC-1 | Security | ask | M1 — producer prose, Proactive controls, Abuse rows | Bug class **V5 Output Encoding & Injection Prevention** (CWE-116/CWE-94-adjacent); OWASP **LLM01**. Merged threat-model row **tm-slo-threat-model-abuse-1**. The merged threat model only modelled the **consumer** (read) side (SEC-1 fence rule). The producer is a NEW **write**-side surface: `/slo-architect` (an LLM) serializes user-controlled idea-doc Top-risks / abuse strings into the `.slo.json`. The runbook mandates conformance + provenance but says **nothing about neutralising user-controlled strings during emission**. Class: **residual — not addressed**. variant-analysis: N/A — contract layer, producer is prose, no code sink (playbook class-defined-at-contract exit). | A contributor's idea-doc Top-risks text is crafted so that when `/slo-architect` hand-emits JSON it stays schema-valid but sets `accepted_residual: true` on a real gap (suppressing it from `/slo-critique`) or stamps `classification: public` on a confidential residual — `deny_unknown_fields` does not catch it because no unknown field is used. The poisoned file then drives every downstream consumer. | M1 producer prose MUST state: emit via a structural JSON serializer (or escape equivalently); idea-doc/user-controlled text is rendered ONLY into descriptive string fields, NEVER allowed to choose `id`, `classification`, `accepted_residual`, or `status` (those are author-controlled). Add an abuse BDD row + a structural assertion that the producer prose states this. Note as a gap against `/slo-architect`'s own threat model (producer write-side was unmodelled). |
| SEC-2 | Security | defer | §6 Out-of-scope | Public-repo recon residual (V8/CWE-200, tm-slo-threat-model-abuse-4/5/6) — correctly and explicitly excluded; parked in `LESSONS-BACKLOG.md` Row 2. Not re-raised; informational. | (inherited, design-accepted) | No plan change. Remains the parked follow-up; the producer runbook rightly does not absorb it. |
| DESIGN | Design | n/a | — | N/A — no UI surface (skill prose + JSON fixtures + Rust tests). | — | — |

## Disposition

- **ENG-1, ENG-2, SEC-1** — accepted by the driver as in-scope plan corrections
  (they fix over-claims / a factual error / a missing producer-side control;
  no new milestone, no new public surface). Folded into the runbook directly.
- **CEO-1** — hold-scope, informational; no change (accepted property of a
  prompt-based pack, true of the merged wedge).
- **SEC-2** — defer; remains the parked follow-up, correctly out of scope.
