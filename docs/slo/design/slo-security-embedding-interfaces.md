# Interfaces — slo-security-embedding

Interfaces that downstream milestones must not rename or reshape without an explicit migration entry. Stability levels:

- **`stable`** — frozen for the life of this feature and beyond; breaking change requires a migration entry in the runbook.
- **`evolving`** — may change with a migration entry; not renamed casually.
- **`internal`** — fair game for refactor.

## Skill invocation verbs

| Skill | Stability | Notes |
|---|---|---|
| `/slo-ideate` | `stable` | User-facing; part of the canonical Think→Ship flow. M1 extends the prompt but keeps the verb. |
| `/slo-architect` | `stable` | M1 extends to emit `SECURITY.md` + `docs/slo/design/<slug>-threat-model.md` and to set `security_libs_required` + `ai_component` frontmatter. Verb unchanged. |
| `/slo-plan` | `stable` | M2 extends the Contract Block with three required rows. Verb unchanged. |
| `/slo-critique` | `stable` | M3 rewrites the `personas/security.md` prompt. Verb and persona-file names unchanged. |
| `/slo-verify` | `stable` | M4 adds Pass 4 (security). Verb and three-pass ordering unchanged; Pass 4 is additive. |
| `/slo-execute`, `/slo-retro`, `/slo-ship`, `/slo-research`, `/slo-tla`, `/slo-resume`, `/slo-freeze`, `/slo-second-opinion` | `stable` | Not directly edited in Phase 1. |
| `/slo-threat-model` (new, Phase 2) | `stable` once shipped | Added by a separate runbook; placeholder commitment here: output is OTM 0.2.0 JSON + Markdown companion. |
| `/slo-security-test` (new, Phase 3) | `stable` once shipped | Placeholder: emits SARIF 2.1.0 merged report + a `docs/security/test-report-M<N>.md` summary. |
| `/slo-sec-libs` (new, Phase 4) | `stable` once shipped | Placeholder: reads CycloneDX 1.6+ declarations, emits `docs/security/library-recommendations.md`, files gap issues to SLO-intake-repo by default. |

## Artifact file paths

| Path | Stability | Owner | Notes |
|---|---|---|---|
| `SECURITY.md` (target repo root) | `stable` | generated by `/slo-architect` M1 | Project-wide security rules. Read by every downstream agent before generating code. |
| `docs/slo/design/<slug>-overview.md` | `stable` | `/slo-architect` | Frontmatter fields `tla_required`, `security_libs_required`, `ai_component` are load-bearing — M1 adds the latter two. |
| `docs/slo/design/<slug>-threat-model.md` | `stable` | `/slo-architect` M1 (or `/slo-threat-model` Phase 2 if invoked) | STRIDE table + abuse cases + compliance mapping. OTM 0.2.0 JSON companion once Phase 2 ships. |
| `docs/slo/design/<slug>-stack-decision.md` | `stable` | `/slo-architect` | No schema change in Phase 1. |
| `docs/slo/design/<slug>-interfaces.md` | `stable` | `/slo-architect` | This file's shape is the contract. |
| `docs/RUNBOOK-<slug>.md` | `stable` | `/slo-plan` | M2 adds three required rows to Contract Block (see "Runbook schema" below). |
| `docs/slo/critique/<slug>.md` | `stable` | `/slo-critique` | Existing format. M3 hardens the security persona; finding schema unchanged. |
| `docs/slo/verify/<slug>-m<N>.md` | `stable` | `/slo-verify` | M4 adds a "Pass 4 — security" section; existing sections unchanged. |
| `docs/slo/lessons/<slug>-m<N>.md` | `stable` | `/slo-retro` | No schema change. |
| `docs/slo/completion/<slug>-m<N>.md` | `stable` | `/slo-retro` | No schema change. |
| `docs/security/` (new directory) | `stable` | Phases 2–4 | Reserved for `threat-model.md` OTM JSON sidecars, `test-report-M<N>.md`, `library-recommendations.md`. |

## Runbook schema additions (Milestone Contract Block, added in M2)

Three required rows are added to the Contract Block of every milestone:

| Row | Required when | Stability | Example |
|---|---|---|---|
| **Data classification** | always | `stable` | `Internal (config paths + skill prompts; no PII, no secrets, no customer data)` |
| **Proactive controls in play** | always | `stable` | `C5 secure_boundary (input validation at skill boundary), C10 secure_errors (no raw panic messages in user-visible output)` |
| **Abuse acceptance scenarios** | when the milestone introduces a new surface (endpoint, IPC handler, file path written, outbound request, subprocess invocation) | `stable` | `Attacker supplies a crafted SKILL.md path traversal; /slo-execute rejects it with a Path safety violation before reading.` |

The classification vocabulary is fixed: `Public`, `Internal`, `Confidential`, `Restricted`. Proactive-control citations use the SunLitSecureLibraries naming (`C1`–`C10`) when the target repo uses those crates; for other stacks, agents cite OWASP Proactive Controls category names directly.

## SKILL.md frontmatter additions

The following new frontmatter keys may appear in `docs/slo/design/<slug>-overview.md`. They are **read** by downstream skills:

| Key | Type | Set by | Read by | Notes |
|---|---|---|---|---|
| `tla_required` | bool | `/slo-architect` | `/slo-tla` | Unchanged from today. |
| `security_libs_required` | bool | `/slo-architect` M1 | `/slo-plan`, `/slo-critique`, `/slo-sec-libs` (Phase 4) | `true` when the target repo uses Rust-axum web services, Pulumi/AWS infra, or multi-tenant auth flows — i.e., when Hulumi or SunLitSecureLibraries are candidates. |
| `ai_component` | bool | `/slo-architect` M1 | `/slo-plan`, `/slo-critique` | `true` when the system under design invokes or embeds an LLM. Triggers the MITRE ATLAS + OWASP LLM Top 10 + NIST AI RMF citation triad. |
| `compliance` | list of strings | `/slo-architect` M1 (or user-authored) | `/slo-architect`, `/slo-plan`, `/slo-critique` | Opt-in framework column set. Allowed values today: `soc2`, `asvs`, `gdpr`, `hipaa`, `pci-dss`, `nist-800-53`, `iso-27001`. Defaults: `[soc2, asvs]` when absent. |

## Persona file paths (critique)

| Path | Stability | Notes |
|---|---|---|
| `skills/slo-critique/personas/security.md` | `evolving` | M3 rewrites the content around class elimination + variant analysis; filename unchanged. |
| `skills/slo-critique/personas/ceo.md`, `eng.md`, `design.md` | `stable` | Not edited in Phase 1. |

## Shell-out contracts (introduced by future phases — placeholder only)

These are not implemented in Phase 1; they are named here so Phase 2–4 architects do not re-litigate the decisions.

| Contract | Stability | Notes |
|---|---|---|
| SecOpsTM Python subprocess | `evolving` (Phase 2) | Invoked with OTM-compatible input; output validated against `otm_schema.json` before use. |
| Semgrep CE CLI invocation | `evolving` (Phase 3) | `--sarif --sarif-output=<path>`; rules bundled under `skills/slo-security-test/rules/`. |
| ast-grep CLI invocation | `evolving` (Phase 3) | Native SARIF since 0.40.0; CLI version pinned in the Phase 3 stack decision. |
| `gh issue create --repo <owner>/<intake>` | `evolving` (Phase 4) | Default intake repo. Upstream filing behind `--file-upstream` flag with 40/hr cap. |

## What is explicitly NOT an interface

- **Internal wording of skill prompts.** The body text of `SKILL.md` files can be rewritten; only the frontmatter (`name`, `description`) and the user-facing verb are interface.
- **Critique finding row order or wording.** The table shape (id, persona, category, section, finding, scenario, recommendation) is an interface; specific phrasings inside a row are not.
- **Threat-model Markdown prose.** The OTM 0.2.0 JSON sidecar (Phase 2) is the interface. The Markdown companion is human-readable documentation and may be reformatted.
- **`docs/security/` subdirectory layout beyond the three filenames named above.** Sub-organization may evolve.
