# Verification Report — scanner-orch (M1–M5)

> Generated by `/slo-verify scanner-orchestration` on 2026-04-26.
> Single-report shape covering all 5 milestones because the runbook is a coherent skill-pack delivery (one skill, five capability layers); milestone-by-milestone verification reports would over-fragment for a markdown-only target.
> Scope: runtime QA on the scanner-orchestration runbook's deliverables. Passes 1–3 are exercised via the structural-contract test suite (the pattern this skill pack uses for markdown-only deliverables — runtime behavior of skills is interpreted by Claude Code and tested via smoke + dogfood, not via a parallel Rust harness). Pass 4 (security: supply-chain + variant-analysis) is exercised end-to-end here.

## What was exercised

| Pass | Scenario | Category | How exercised | Result | Evidence |
|---|---|---|---|---|---|
| **1** | M1 — parser scaffold + scope rule (8 BDD scenarios + 5 E2E + 7 fixtures, reframed) | happy path | `cargo test -p sldo-install --test e2e_scanner_orch_m1` | ✅ **PASS** (21/21 tests) | M1 lessons file documents the structural-contract pivot: BDD/E2E scenarios are exercised as structural-contract assertions; runtime behavior validated via deferred smoke tests + `/slo-verify` discipline. Test runtime: 0.00s incremental. |
| **1** | M2 — stack detection + cache fetch + filter (12 BDD + 7 E2E) | happy path | `cargo test -p sldo-install --test e2e_scanner_orch_m2` | ✅ **PASS** (22/22 tests) | Pinned-SHA discipline + manifest priority + filter intersection all asserted via reference docs. Stack-detection contract is `stable`. |
| **1** | M3 — emission + workflow safety contract (10 BDD + 9 E2E) | happy path | `cargo test -p sldo-install --test e2e_scanner_orch_m3` | ✅ **PASS** (20/20 tests) | Workflow-template-as-frozen-fixture pattern: each safety property gets its own `#[test]` (no `pull_request_target`, `permissions: {}`, SHA-pinned actions, `fetch-depth: 0`, `SEMGREP_RULES` env var, no `secrets.*`, no `--autofix`, no `--config`). |
| **1** | M4 — manifest schema + preview-mode UX (12 BDD + 9 E2E) | happy path | `cargo test -p sldo-install --test e2e_scanner_orch_m4` | ✅ **PASS** (17/17 tests) | Schema v1.0 documented; defensive-design framing asserted; PCI 6.2.3 citation correctness checked; `cwes_uncovered = cwes_claimed \ cwes_actually_covered` set-algebra rule documented. |
| **1** | M5 — re-derivation triggers + diff PR + dogfood (13 BDD + 10 E2E) | happy path | `cargo test -p sldo-install --test e2e_scanner_orch_m5` | ✅ **PASS** (19/19 tests) | Four trigger predicates documented; compound-trigger coalescing rule; argv-list `gh pr create` discipline; no-`--repo` flag; max 1 PR per invocation. |
| **2** | M1 — `errors_on_missing_threat_model` | invalid input | Asserted via `skill_md_documents_missing_file_behavior` test | ✅ **PASS** | SKILL.md documents non-zero exit + clear stderr on missing threat-model. Runtime-invocation verification deferred (would require live `claude /slo-sast` exec). |
| **2** | M1 — `returns_empty_list_when_no_cwes_named` | empty state | Asserted via `skill_md_documents_empty_list_behavior` test | ✅ **PASS** | `[]` output + exit 0 documented. M2 fallback behavior documented (`default-fallback` strategy). |
| **2** | M2 — `git_unavailable_clean_error` | dependency failure | Asserted via test references in `e2e_scanner_orch_m2.rs` (per-keyword content check on SKILL.md) | ✅ **PASS** | "git not on PATH" error path documented in SKILL.md M2 section. |
| **2** | M2 — `refuses_tag_reference_in_pinned_sha` | invalid input | Asserted via `pinned_sha_doc_documents_sha_only_enforcement` | ✅ **PASS** | Reference doc explicitly forbids tag, branch, short SHA. |
| **2** | M2 — `refuses_yaml_with_billion_laughs` (SEC-2) | abuse case | Asserted via `skill_md_documents_yaml_parser_safety` | ✅ **PASS** | `serde_yaml_ng` default-safe parsing documented; entity-expansion defense documented. |
| **2** | M3 — `prompt_injection_in_threat_model_does_not_taint_workflow` | abuse case | Workflow template parsed and inspected via `workflow_template_uses_pull_request_not_pull_request_target` + `workflow_template_uses_sha_placeholders_only` + 7 other safety property tests | ✅ **PASS** | Architectural defense via static template + parameter-only substitution. Comment-tolerant assertions exclude rationale-comment mentions. |
| **2** | M3 — `refuses_emission_when_dot_semgrep_is_symlink` (SEC-1) | abuse case | Asserted via `skill_md_documents_symlink_traversal_defense` | ✅ **PASS** | SEC-1 symlink-traversal defense documented in M3 SKILL.md section + extended to M4 manifest write site. |
| **2** | M4 — `manifest_values_are_regex_validated_only` (SEC-4) | abuse case | Asserted via `manifest_schema_documents_validation_rules` | ✅ **PASS** | Regex `^CWE-\d+$` and `^[0-9a-f]{40}$` validation rules documented. |
| **2** | M4 — `preview_mode_required_on_first_install` | invalid state | Asserted via `skill_md_documents_preview_mode` + `skill_md_documents_mixed_state_trigger` | ✅ **PASS** | First-install / re-derivation / mixed-pre-existing-state distinctions documented (ENG-3 extension applied). |
| **2** | M5 — `no_drift_no_pr` | empty state | Asserted via `triggers_doc_documents_*` tests | ✅ **PASS** | Quiet path documented in triggers reference doc. |
| **2** | M5 — `gh_unavailable_clean_error` | dependency failure | Asserted via SKILL.md reference to "use existing user auth or fail with clear error" | ✅ **PASS** | Documented in M5 anti-patterns. |
| **3** | All milestones | partial failure / concurrency | N/A | **N/A** | Single-process, single-invocation skill; no concurrent actors share state; no failure-injection points; no resource leases. `tla_required: false` in `<slug>-overview.md` is the architectural justification. |
| **4a** | Vulnerable deps | supply chain | `cargo audit --no-fetch` | ✅ **PASS (clean)** | 121 deps scanned against 1058 RustSec advisories. Exit 0. |
| **4b** | License + advisory + source + bans | supply chain | `cargo deny check` | ⚠️ **PRE-EXISTING FINDING** (not scanner-orch's regression — see Bugs Found §1) | `advisories ok, bans ok, licenses FAILED, sources ok`. License rejection on `winsafe v0.0.19` (transitive dep of `which v7.0.3`). Scanner-orchestration adds zero new deps; `git diff HEAD Cargo.toml Cargo.lock` is empty. |
| **4c** | GitHub Actions safety | variant analysis | `semgrep --config p/github-actions references/sast/scanner-orch-workflow-template.yml` | ✅ **PASS (0 findings)** | 3 rules run on the emitted workflow template — the load-bearing artifact for `tm-scanner-orchestration-abuse-3`. |
| **4d** | Security audit (general) | variant analysis | `semgrep --config p/security-audit crates/sldo-install/tests/e2e_scanner_orch_m{1..5}.rs` | ✅ **PASS (0 findings)** | 2 multilang rules applied to 5 new test files. (Rust-specific Semgrep coverage is limited; `p/cwe-top-25` would add more, but the new code is purely structural-contract test scaffolding — `fs::read_to_string` + `assert!` patterns — no SQL / shell / network surface.) |
| **4e** | DAST | runtime exposure | N/A | **N/A** | No compiled service / no `docker-compose.yml` / no smoke service in this runbook. Markdown-only skill pack. |
| **4f** | PII-pattern scan | biz-pack discipline | N/A | **N/A** | No `docs/biz-public/` in repo (this runbook is unrelated to the biz pack). PII-pattern scan only applies when biz-pack artifacts are present. |
| **4g** | ast-grep variant analysis | variant analysis | `which ast-grep` returns nothing | ⏭️ **SKIPPED** | Tool not installed. Per Pass 4 tool-optional rule, skipped row emitted; no phantom finding. Install hint: `cargo install ast-grep --locked`. |

## Bugs found

| id | severity | scenario | regression test | status |
|---|---|---|---|---|
| **VERIFY-1** | low (pre-existing, not introduced by scanner-orch) | Pass 4b — `cargo deny check` licenses | NOT NEW — pre-existing workspace state | **Out of scope for scanner-orch.** `winsafe v0.0.19` (transitive dep of `which v7.0.3`) license rejected because not in `deny.toml`'s `allow:` list. Scanner-orchestration M1–M5 added zero dependencies (`git diff HEAD Cargo.toml Cargo.lock` is empty), so this is a pre-existing baseline issue. **Recommendation**: surface as a separate workspace-hygiene issue (file as a follow-up, not as scanner-orch regression). Filing in this report for traceability — the verify pass found it; downstream triage decides whether to fix in this PR or a sibling cleanup PR. |

**No new bugs introduced by scanner-orchestration M1–M5.** The single Pass 4 finding pre-dates this runbook and has no causal relationship to the new code (verified by zero-dep delta).

## Environment

- **OS**: macOS 25.4.0 (Darwin arm64).
- **Rust**: stable (per `~/.cargo/bin/cargo`).
- **Cargo binaries**: `cargo`, `cargo-audit`, `cargo-deny` (all present at `~/.cargo/bin/`).
- **Semgrep**: 1.156.0 (via Homebrew at `/opt/homebrew/bin/semgrep`).
- **`ast-grep`**: not installed.
- **`claude` CLI**: present (used implicitly by skill invocation in smoke tests; not invoked at this verify layer to avoid API token consumption).
- **Stack detected**: Rust workspace (Cargo.toml at root). No Node, Python, or Go.
- **`docs/biz-public/`**: not present.

## Coverage gaps

- **Live `/slo-sast` runtime invocation against a fixture target repo** — would require actually executing `claude /slo-sast <slug>` and observing real stdout/stderr. Skipped to avoid API budget consumption during the verify pass; the structural-contract test pattern + smoke-test discipline (deferred to user-driven exercise) is the SLO skill-pack convention. **Risk**: a SKILL.md content drift between authoring and runtime interpretation by a future Claude version is not directly tested here. Mitigation: SLO's broader skill-pack invariant (Claude Code reads SKILL.md and follows it; sldo-install manifest detects between-install tampering) plus the runbook's `/slo-ship` smoke step.
- **Real wedge-validation** (CEO-1) — three threat-model fixtures exercising the parser scope rule against representative OSS threat models. Documented in M2's smoke-test step; deferred until the first real `semgrep_rules_sha` bump-PR (the placeholder all-zero must be replaced first).
- **Real-Semgrep dry-run smoke** (ENG-1) — pipe the emitted workflow through `semgrep ci --dry-run --no-rewrite-rule-ids`. Documented in M3's smoke tests; deferred until the action SHAs are bumped from placeholder.
- **Dogfood execution** (M5) — run `/slo-sast` against this SLO repo. Documented in M5's BDD; deferred for the same reason (placeholder SHAs).
- **Full `cargo test --workspace` cold-build verification** — substituted with `cargo check --workspace` + per-crate `cargo test -p sldo-install` during execute due to ~10 min cold-build wall time. Per-crate evidence + targeted M1–M5 suites all green; full-workspace re-run pending environmental conditions.

## Pass 4 budget

Total Pass 4 wall time: ~8 minutes (dominated by `cargo audit` advisory-DB fetch + license-check + Semgrep config download). Within the runbook's nightly-cadence-or-shorter target. Subsequent invocations with `--no-fetch` flags should drop to ~2 minutes.

## Verdict

**All 99 structural-contract tests across M1–M5 are green.** Pass 4 finds one pre-existing license issue unrelated to scanner-orchestration's new code. **No new bugs introduced by scanner-orch M1–M5.**

The runbook is verified at the structural-contract + Pass 4 (security) layers. Live runtime invocation of `/slo-sast` is appropriately deferred to smoke testing (post-bump-PR) and to the `/slo-ship` step.

## Handoff

Suggest `/slo-retro M5` to close out the final milestone, then `/slo-ship` to open the PR. Note in the retro lessons: the "pre-existing `cargo deny` license finding" found here should be filed as a separate workspace-hygiene issue, not bundled with the scanner-orchestration PR.
