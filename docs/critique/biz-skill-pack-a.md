# Critique Summary — biz-skill-pack-a

Run date: 2026-04-25. Runbook reviewed: [RUNBOOK-BIZ-SKILL-PACK-A.md](../RUNBOOK-BIZ-SKILL-PACK-A.md).

> **Timing note**: This critique runs *post-execution* — all four milestones are already `done` with 42 structural-contract tests green. Findings below are framed as *what would have been pre-execution flags + what to fix in a small follow-up runbook*. Auto-fix is reserved for mechanical issues; substantive findings are `ask` — surfaced for user review and resolution before or after `/slo-ship`.

## Overall disposition

The runbook landed cleanly: 4 advisor skills, 10 shared reference files, 42 structural-contract tests, 4 lessons + completion files, single CLAUDE.md catalog edit. CEO has one ship-strategy `ask`. Eng has three `ask` findings concentrated around the LLM-judgment-vs-structural-test gap and the cost-baseline placeholder. Security has two `ask` findings — one on the oneNDA placeholder window, one on LLM-judgment residual at gate-4 + IR35. Design persona skipped (no UI surface — biz pack is Markdown skills + tests + reference docs only). No `auto-fix` findings — the runbook's contracts and tests are internally consistent.

## Findings

| id | persona | category | runbook section | finding | concrete scenario | recommendation |
|----|---------|----------|-----------------|---------|-------------------|----------------|
| f1 | CEO | ask | Runbook Metadata + Milestone Tracker | Runbook A ships 4 advisor skills as one PR. The wedge (M1 `/slo-legal`) was originally framed as the standalone validation step before M2-M4 replicate the pattern. With execution complete, the question shifts: ship Runbook A as **one PR** (more skills land at once; reviewer sees the cluster) or as **four sequential PRs** (each milestone reviewable in isolation; faster individual feedback). | Sherif opens a 30+ file PR for Runbook A. A reviewer (himself, or a future contributor) skims the diff, says "this is too much to review in one sitting, can you split", and the work re-ships as four PRs anyway with the cost of git churn. OR: Sherif opens four PRs and the cross-skill citation tests in M2/M3/M4 fail in isolation because each PR's predecessor isn't merged yet, requiring a coordination cost that one PR avoided. | **Hold** — recommend ONE PR for Runbook A. Reasoning: (a) cross-skill citation tests (M2/M3/M4) are inherently multi-skill and don't make sense in isolation; (b) the locked decisions (GDPR hard-block, JPP Law baseline, oneNDA verbatim) are pack-level not skill-level; (c) the four advisor skills are a coherent advisor cluster, and shipping a partial cluster confuses founders. If the user prefers split PRs, M1 is the only milestone that's defensible as a standalone PR (proves the pattern); M2/M3/M4 should still be one bundle. |
| f2 | eng | ask | M1 Definition of Done + `references/biz/cost-baseline-jpp-law-2026.md` placeholder GBP figures | The cost-baseline file ships in M1 with literal `placeholder — replace with current JPP Law fixed fee at retrieval time` strings in the Price column. The smoke checklist documents "manually fetch and replace before first use", but no structural-contract test asserts the placeholders are gone. A founder cloning the SLO repo could install the skill, run `/slo-legal draft contractor-sow`, and the ROI block in the output would render with the literal placeholder string — defeating the auditable-cost-baseline goal of locked decision 2026-04-25. | Founder runs `cargo build -p sldo-install --release && ./target/release/sldo-install`, then in Claude Code invokes `/slo-legal draft contractor-sow` for a fixture deal. The output's "Cost baseline (provenance)" footer reads "a UK solicitor would charge approximately £placeholder — replace with current JPP Law fixed fee at retrieval time for an equivalent contractor-sow". Founder loses confidence in the skill's polish. | Add a structural-contract test (in a small follow-up M1.5 runbook OR before ship) `cost_baseline_placeholders_replaced`: scan `references/biz/cost-baseline-jpp-law-2026.md` for the literal string `placeholder — replace with current JPP Law` and assert ZERO matches. The test will fail in M1's current state — that's the signal to fetch JPP Law's current pricing and update before ship. Alternatively: change the placeholder string to a real-but-clearly-marked-stale GBP figure (e.g., `£0 (PLACEHOLDER — replace before use)`) so the cost-block is readable but the warning is still visible. |
| f3 | eng | ask | M2 BDD `cross_skill_advisor_pattern_replicated` + `e2e_biz_a_m4.rs` `cross_skill_advisor_pattern_replicated_all_four` | The cross-skill citation test asserts each predicate id appears as a substring in each advisor SKILL.md. It does NOT assert the citation is in a hard-block-discussion context (vs. metadata only — e.g., a TOC line or a frontmatter description). A future skill author could move predicate-id citations out of the operative hard-block prose into a single mention in the description string and the test would still pass. | A new contributor, refactoring `slo-legal/SKILL.md` to "tighten the prose", moves all four predicate-id mentions into a single description-frontmatter line: `description: skill applies gate-1-regulated, gate-2-deal-value-over-5k, gate-3-counterparty-has-lawyer-or-their-paper, gate-4-gdpr-document predicates`. The body no longer discusses any predicate. `cross_skill_advisor_pattern_replicated` passes; `/slo-legal draft contractor-sow` no longer enforces the hard-blocks at runtime because the skill prose doesn't tell the LLM to check. | Tighten the test in a follow-up runbook: parse the SKILL.md body, locate the "Hard-block gates" or equivalent heading section, assert each predicate id appears WITHIN that section. Or, more loosely: assert each predicate id appears at least N times in the SKILL.md body where N reflects "title mention + body discussion + routing rule" (typically N≥2 per predicate). Either improvement closes the loophole. Defer to a small `biz-pack-test-hardening` follow-up runbook; do not gate the current ship. |
| f4 | eng | ask | M1-M4 — `references/biz/templates/onenda-uk.md` placeholder + `e2e_biz_a_m1.rs::onenda_template_placeholder_or_pinned_hash` | The placeholder mechanism is a known compromise documented in M1's lessons file, but the test allows ANY content as long as the marker `ONENDA-UK-PLACEHOLDER` is absent (the future-pinned-hash branch). Until the canonical hash is pinned (deferred to a follow-up), the test only enforces "marker present". A contributor who removes the marker and replaces with arbitrary content (e.g., a GPL-licensed NDA, or an attacker-controlled "canonical" oneNDA bytes) bypasses the placeholder branch and the test passes. | Malicious or unwary contributor opens a PR titled "land canonical oneNDA bytes — fetched from onenda.org". The PR removes the `ONENDA-UK-PLACEHOLDER` marker and replaces the file with arbitrary content (could be canonical, could be subtly altered, could be an entirely different NDA). The test passes because the marker is gone and the assertion path is "either marker OR pinned hash" — but no hash is pinned yet. The PR merges; `/slo-legal draft nda` now renders attacker-controlled prose verbatim under a CC BY-ND 4.0 attribution that no longer applies. | Pin the canonical oneNDA SHA-256 in a small follow-up runbook BEFORE any PR is allowed to remove the placeholder marker. Process: project owner fetches canonical bytes from onenda.org, computes SHA-256 locally, opens a single PR that pins the hash in `e2e_biz_a_m1.rs` AND removes the marker from the placeholder file in lockstep. Until that follow-up ships, additional protection: tighten `onenda_template_placeholder_or_pinned_hash` to require the marker is present (no future-pinned-hash branch yet) — fail-closed on premature marker removal. Then loosen when the canonical hash is pinned. |
| f5 | security | ask | M1 oneNDA template placeholder mechanism + `references/biz/templates/onenda-uk.md` | **Class**: V10.3 Code Integrity — Third-party content verification. **Threat-model row**: `tm-biz-abuse-2` (oneNDA template tampering). **Status**: mitigated → **residual during placeholder window**. The placeholder-or-pinned-hash test is correct but incomplete: between M1 ship and the follow-up that pins the canonical hash, ANY content that lacks the placeholder marker passes the test. **Variant analysis**: `rg -n 'PLACEHOLDER' references/` returns 1 hit (the oneNDA placeholder itself) — pattern is unique to this surface; no neighbouring code paths have the same gap. | A maintainer pushes a "canonical oneNDA bytes" PR. The PR replaces the placeholder file with content that is 99% canonical oneNDA but has a single-clause modification weakening the IP-confidentiality scope. The structural test passes (marker is gone). Reviewer skims the diff seeing `+~600 lines, -50 lines` and approves under "the bytes look like an NDA". The bad clause is now in every `/slo-legal draft nda` output until someone notices. Impact: founders ship NDAs with weakened protections; downstream legal exposure on real engagements. | Same as f4's recommendation: (a) follow-up runbook pins the canonical hash; (b) interim hardening — tighten the test to require the marker is present (no pinned-hash branch yet) so removing the marker fails the test until the canonical hash is pinned in lockstep. The two changes ship together as one small follow-up runbook (estimated 1 day work + manual oneNDA byte fetch + verification). |
| f6 | security | ask | M1-M4 — gate-4 GDPR hard-block + M4 IR35 / CEST factors triage | **Class**: OWASP LLM08 Excessive Agency / OWASP LLM09 Over-reliance on output. **Threat-model rows**: `tm-biz-abuse-3` (GDPR-block circumvention), `tm-biz-abuse-4` (silent-wrongness on IR35 triage). **Status**: residual on LLM-judgment accuracy. The structural-contract tests assert the gate-4 routing prose and the IR35 seven-trigger list are documented in skill prose and reference files. They do NOT exercise the LLM's runtime classification of marginal cases (e.g., "I want a one-pager about how we use customer data" — is that a privacy notice? "Contractor full-time exclusive but with a substitution clause" — is that IR35-inside?). The smoke checklists name marginal-case fixtures but smoke is manual; no automated regression suite exercises judgment. **Variant analysis**: the same residual applies to ALL FOUR advisor skills. `rg -n 'gate-4-gdpr-document' skills/slo-*/SKILL.md` returns one mention per advisor SKILL (4 total) — variant count = 4; risk is multiplied across the cluster. | Founder asks `/slo-legal draft tos` for a SaaS that processes some customer email addresses for transactional purposes only. The LLM evaluates: is this a GDPR document? Email addresses are personal data; T&Cs reference data handling; gate-4 should fire. But the prompt is shaped as "T&Cs not privacy notice" — the LLM might classify it as gate-4-out-of-scope and produce a draft. The draft contains a brief data-handling clause that mis-states the lawful basis. Founder ships. ICO complaint surfaces 18 months later. £17.5M PECR ceiling now applies under DUAA Stage 3. The structural test asserted gate-4 is documented; runtime classification was the failure mode. | Build a runtime BDD harness in a follow-up runbook (`biz-pack-judgment-tests`): a fixture set of marginal-case prompts (10-20 per advisor) invokes the skill via `claude` CLI in a scripted session, captures the artifact, and asserts the artifact frontmatter `gates_fired:` matches the expected gate set per fixture. This is a `claude`-CLI-driven test (similar to existing `e2e_research_*` tests but for skills, not the research binary). Cost: ~1-2 weeks. Defer to post-Runbook-B1; the manual smoke checklist + `/slo-critique` security-persona variant analysis on every B1/B2/C milestone are the interim compensating controls. |
| f7 | design | N/A | — | No UI surface. Design persona skipped. | — | — |

## Auto-fixes applied

None. All findings are non-mechanical (test design, security hardening, ship strategy) and require user decision.

## Asks — disposition (auto mode)

The user authorized `/slo-critique biz-skill-pack-a then /slo-ship` — i.e., run critique, surface findings, then ship. Findings are NOT applied inline because the work is post-execution and substantive changes belong in a follow-up runbook, not edits to a shipping branch.

- **f1 — confirmed hold ONE PR.** Runbook A ships as a single PR. Cross-skill tests + locked decisions are pack-level; splitting would create coordination cost without review benefit.
- **f2 — DEFER to a small `biz-pack-cost-baseline-refresh` follow-up runbook.** Add the placeholder-replaced test + manual JPP Law fetch + commit. Estimated cost: 30 min. Recommended as the FIRST follow-up after Runbook A ships, ideally same day.
- **f3 — DEFER to a `biz-pack-test-hardening` follow-up runbook.** Tighten cross-skill citation test to assert hard-block-context placement, not just substring presence. Bundle with f4's interim hardening.
- **f4 — DEFER to two follow-ups.** Interim: tighten `onenda_template_placeholder_or_pinned_hash` to require marker is present (fail-closed). Permanent: pin the canonical oneNDA SHA-256 once project owner has fetched and verified canonical bytes from onenda.org. Bundle interim hardening with f3.
- **f5 — same disposition as f4.** Security framing of f4; same fix.
- **f6 — DEFER to a `biz-pack-judgment-tests` follow-up runbook (post-Runbook-B1).** Build the `claude`-CLI-driven runtime harness for marginal-case fixtures across all four advisor skills. Compensating controls in place: manual smoke checklists, `/slo-critique` security variant analysis on every milestone of Runbook B1/B2/C.

Three follow-up runbooks are queued from this critique:

1. **`biz-pack-cost-baseline-refresh`** (FIRST — same day as Runbook A ship) — replaces JPP Law placeholders + adds asserts-replaced test.
2. **`biz-pack-test-hardening`** (after f1's PR merges) — tightens cross-skill citation test (f3) + tightens oneNDA placeholder test (f4 interim).
3. **`biz-pack-onenda-canonical`** (after f4 interim ships) — pins canonical oneNDA SHA-256 + removes placeholder marker in lockstep. Requires manual oneNDA byte fetch by project owner.
4. **`biz-pack-judgment-tests`** (after Runbook B1 ships) — runtime BDD harness for LLM-judgment marginal cases across all 7+ advisor skills (4 advisors + 3 if biz pack expands).

## Hold-scope

- **f1 — confirmed.** Runbook A ships as one PR.

## Reduce-scope

None. Pack is at minimum size for the locked design decisions.

## Defer

- f2, f3, f4, f5, f6 — all deferred to named follow-up runbooks per dispositions above.

## Net BDD count after critique

No structural-contract tests added or removed. Total still 42 (M1: 10, M2: 11, M3: 10, M4: 11). The four follow-up runbooks above will add tests when they ship.

## Next action

`/slo-ship` runs next per user direction. The PR description should call out:

- Locked decisions: GDPR broad hard-block, JPP Law cost baseline, UK only v1, oneNDA verbatim.
- Known deferrals: oneNDA SHA-256 hash check, `/slo-verify` PII-pattern scan, JPP Law GBP figures (placeholder), critique-driven test hardening.
- Critique findings (f1-f6) and their planned follow-up runbooks (4 named items above).
- 42 structural-contract tests green; full baseline (~330 tests) green.
