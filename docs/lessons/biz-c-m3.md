# Lessons — biz-c M3 (`/slo-founder-check` + final pack closure)

NEW skill (~130 lines), 12 tests green including pack-level catalog completeness, references/biz/ non-discovery regression, predicate-id immutability through Runbook C, advisor cluster citation regression. CLAUDE.md catalog edit appending all 3 C skills. 12-question self-assessment + 4-tier worst-case-runway worksheet + optional YC application prep. Mental-health / personal-finance routings to professional support — out of skill scope.

## Pack-level state at end of all 4 runbooks
15 biz skills shipped: 4 advisors (A) + 4 strategy generators (B1) + 4 execution generators (B2) + 3 team generators (C). Total structural-contract tests across the pack: 42 + 32 + 34 + 29 = 137. All green. Installer discovers all 15 plus 14 pre-existing pack skills + 2 SAST skills (parallel agent) = 31 skills total at sldo-install --dry-run.

## Locked decisions through all runbooks (immutable)
- 4 triage-gate predicate IDs unchanged from Runbook A M1.
- GDPR broad hard-block on `draft` (locked 2026-04-25).
- JPP Law cost baseline (locked 2026-04-25).
- UK only in v1 (canonical "v1 supports UK only" error).
- oneNDA placeholder pending canonical-bytes pin (deferred follow-up).
- Two-tier output convention enforced + PII-pattern scan in /slo-verify Pass 4.
- Cross-skill citation contract: every advisor SKILL.md cites all four predicate IDs.
- archetype enum: advisor | generator (added B1 M1, immutable through C).
