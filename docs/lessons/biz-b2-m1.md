# Lessons — biz-b2 M1 (`/slo-launch`)

## What changed

- NEW `skills/slo-launch/SKILL.md` (~110 lines, generator).
- NEW `crates/sldo-install/tests/e2e_biz_b2_m1.rs` (7 tests).
- ARCHITECTURE.md row.

## Course corrections

- One iteration: forgot to include literal "v1 supports UK only" string in skill prose. Fixed by spelling out the canonical error inline rather than only referencing the file path.

## Recommendations for M2 (`/slo-sales-funnel`)

The funnel-math worksheet is the load-bearing artifact. Tests should assert the seven outbound-email principles are enumerated AND the deal-structure progression (paid trial → recurring → opt-out) is documented.
