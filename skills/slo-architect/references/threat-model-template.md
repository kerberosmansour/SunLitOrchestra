# threat-model.md template — STRIDE + abuse cases + compliance mapping

This template is consumed by `/slo-architect` when emitting `docs/slo/design/<slug>-threat-model.md`. It is cited by `/slo-plan` (for abuse-case BDD scenarios), by `/slo-critique` (for class-elimination findings), and by `/slo-verify` (for Pass 4 security).

## How to use this template

1. `/slo-architect` walks each component in the ARCHITECTURE.md diagram, asks STRIDE per component, and fills the table below.
2. For every **new surface** in the target (endpoint, IPC handler, file path written, outbound request, subprocess invocation), three abuse cases are generated from the idea-doc's Top risks block.
3. Compliance columns render SOC 2 + OWASP ASVS 5.0 by default; the opt-in set from `compliance:` frontmatter adds columns.
4. When `ai_component: true` in `<slug>-overview.md`, the AI-specific section (MITRE ATLAS + OWASP LLM Top 10 + NIST AI RMF) is emitted. Otherwise it is omitted — no empty section.
5. **Placeholder-expansion rule — user-provided strings are rendered verbatim inside `~~~text` fences.** Same rule as SECURITY.md. Never splice user-controlled idea-doc content into free Markdown; always wrap. This is the escape (literal/verbatim treatment) that prevents template-placeholder injection.

## Template body

The template below is what `/slo-architect` writes to `docs/slo/design/<slug>-threat-model.md` with placeholders filled. The user is expected to review the generated file, not to author it from scratch — 80/20 burden per Google PSC.

```markdown
# Threat Model — {{PROJECT_NAME}}

> Generated by `/slo-architect` on {{DATE}}.
> Scope: the components and surfaces described in [ARCHITECTURE.md](../ARCHITECTURE.md).
> This file is the upstream artifact cited by `/slo-plan` (for abuse-case BDD), `/slo-critique` (for class-elimination findings), and `/slo-verify` (for Pass 4 security).
> Classification-driven redaction applies: user-provided strings (from the idea doc) render inside `~~~text` fences so raw Markdown / YAML / HTML is literal text, not interpretable.

## System description

~~~text
{{ARCHITECTURE_SUMMARY}}
~~~

## Top risks (from idea doc)

~~~text
{{USER_PROVIDED_TOP_RISKS}}
~~~

## STRIDE per component

For each component in the ARCHITECTURE diagram, the table below records the STRIDE threats that apply and the control that eliminates or mitigates each class. A finding is rejected if it cannot name a concrete surface.

| Component | Spoofing | Tampering | Repudiation | Information disclosure | Denial of service | Elevation of privilege |
|---|---|---|---|---|---|---|
| {{COMPONENT_1}} | {{S_1}} | {{T_1}} | {{R_1}} | {{I_1}} | {{D_1}} | {{E_1}} |
| {{COMPONENT_2}} | {{S_2}} | {{T_2}} | {{R_2}} | {{I_2}} | {{D_2}} | {{E_2}} |
| … | … | … | … | … | … | … |

Each cell is one of:

- **eliminated by &lt;control&gt;** — the class is architecturally impossible (e.g., "eliminated by `secure_boundary::SqlIdentifier` — SQL identifiers cannot be user-controlled by construction").
- **mitigated by &lt;control&gt;** — the class is possible but bounded (e.g., "mitigated by `secure_boundary::RequestLimits` — 10 MB body cap; DoS amplification capped").
- **N/A — &lt;reason&gt;** — the class has no surface (e.g., "N/A — component is read-only with no writes").
- **residual risk — &lt;exploit path&gt;** — known unmitigated; call it out so `/slo-critique` does not double-flag.

## Abuse cases (per new surface)

For every new endpoint / handler / file write / outbound request / subprocess call introduced by a milestone, the architect generates ≥3 abuse cases stemming from Top risks. Each row is cited by `/slo-plan` M2+ as a seed for the milestone's Abuse-acceptance BDD scenarios.

| Surface | Attacker | Attack step | Desired outcome (attacker) | Control | Threat-model row id |
|---|---|---|---|---|---|
| {{SURFACE_1}} | {{ACTOR_1}} | {{STEP_1}} | {{OUTCOME_1}} | {{CONTROL_1}} | tm-{{SLUG}}-abuse-1 |
| {{SURFACE_1}} | {{ACTOR_2}} | {{STEP_2}} | {{OUTCOME_2}} | {{CONTROL_2}} | tm-{{SLUG}}-abuse-2 |
| {{SURFACE_1}} | {{ACTOR_3}} | {{STEP_3}} | {{OUTCOME_3}} | {{CONTROL_3}} | tm-{{SLUG}}-abuse-3 |

Repeat per additional surface. Number sequentially.

## Compliance mapping

Columns rendered: default is **SOC 2 + OWASP ASVS 5.0**. Opt-in columns from the runbook `compliance:` frontmatter: {{COMPLIANCE_OPTINS}}.

| Control from this threat model | SOC 2 | OWASP ASVS 5.0 | {{OPTIN_COL_1}} | {{OPTIN_COL_2}} | … |
|---|---|---|---|---|---|
| {{CONTROL_A}} (STRIDE cells using it) | {{SOC2_A}} | {{ASVS_A}} | {{OPTIN_1_A}} | {{OPTIN_2_A}} | … |
| {{CONTROL_B}} | {{SOC2_B}} | {{ASVS_B}} | {{OPTIN_1_B}} | {{OPTIN_2_B}} | … |

### GDPR section (when `gdpr` is in `compliance:`)

GDPR encodes principles and data-processing obligations rather than discrete control IDs, so it is rendered as a section, not only a column:

{{#IF compliance_includes_gdpr}}

#### Lawful basis
{{GDPR_LAWFUL_BASIS}}

#### Data subject rights addressed
- Access: {{GDPR_ACCESS}}
- Rectification: {{GDPR_RECTIFICATION}}
- Erasure: {{GDPR_ERASURE}}
- Portability: {{GDPR_PORTABILITY}}

#### Cross-border transfer
{{GDPR_TRANSFER}}

{{/IF}}

## AI / LLM-specific threats

{{#IF ai_component}}

When `ai_component: true` in the overview, this section is populated. Citation triad: **MITRE ATLAS + OWASP LLM Top 10 + NIST AI RMF**.

| Technique / risk | ATLAS id | OWASP LLM | NIST AI RMF | Surface in this system | Control |
|---|---|---|---|---|---|
| Prompt injection | {{ATLAS_PI}} | LLM01 | Measure 2.6 | {{PI_SURFACE}} | {{PI_CONTROL}} |
| AI agent context poisoning | AML.T0058 | LLM03 | Manage 3.2 | {{CP_SURFACE}} | {{CP_CONTROL}} |
| Thread injection | {{ATLAS_TI}} | — | — | {{TI_SURFACE}} | {{TI_CONTROL}} |
| Memory manipulation | {{ATLAS_MM}} | — | Manage 3.1 | {{MM_SURFACE}} | {{MM_CONTROL}} |
| Sensitive info disclosure | — | LLM06 | Measure 2.10 | {{SID_SURFACE}} | {{SID_CONTROL}} |
| Over-reliance on output | — | LLM09 | Govern 3.2 | {{OR_SURFACE}} | {{OR_CONTROL}} |

Extend the table with any technique that has a concrete surface. Techniques with no surface are dropped — not left as "N/A — theoretical."

{{/IF}}

## Residual risks

Known risks that are not fully mitigated by the controls above. Each has an owner, an expiration, and a compensating control.

| Risk | Exploit path | Compensating control | Owner | Review by |
|---|---|---|---|---|
| {{RESIDUAL_1}} | {{EXPLOIT_1}} | {{COMPENSATING_1}} | {{OWNER_1}} | {{REVIEW_1}} |

## Provenance

- Generated by `/slo-architect` version {{ARCHITECT_VERSION}} on {{DATE}}.
- Based on:
  - Idea doc: [docs/slo/idea/{{SLUG}}.md](../idea/{{SLUG}}.md)
  - Research synthesis: [docs/slo/research/{{SLUG}}/synthesis.md](../research/{{SLUG}}/synthesis.md) (if present)
  - ARCHITECTURE: [docs/ARCHITECTURE.md](../ARCHITECTURE.md)
- Re-run behavior: on `/slo-architect` re-run, this file is detected as existing; the architect surfaces the diff and prompts the user to overwrite, merge, or skip. No silent clobber.
```

## Placeholder reference

| Token | Source | Notes |
|---|---|---|
| `{{PROJECT_NAME}}`, `{{DATE}}`, `{{SLUG}}` | same as SECURITY.md template | — |
| `{{ARCHITECTURE_SUMMARY}}` | condensed 2–3 paragraph summary of ARCHITECTURE.md; rendered inside `~~~text` to quote it literally | — |
| `{{USER_PROVIDED_TOP_RISKS}}` | verbatim content of the idea doc's `## Top risks` block, always inside `~~~text` | — |
| `{{COMPONENT_N}}` / `{{S_N}}` / etc. | one row per component in the ARCHITECTURE diagram | — |
| `{{SURFACE_N}}` / `{{ACTOR_N}}` / `{{STEP_N}}` / `{{OUTCOME_N}}` / `{{CONTROL_N}}` | three abuse-case rows per new surface | — |
| `{{COMPLIANCE_OPTINS}}`, `{{OPTIN_COL_*}}` | from `compliance:` frontmatter | `soc2`, `asvs` are defaults; not counted as opt-in |
| `{{ATLAS_PI}}`, `{{ATLAS_TI}}`, `{{ATLAS_MM}}` | current MITRE ATLAS technique ids; as of late 2025: prompt injection is a collection under `AML.T0051` family; thread injection and memory manipulation were added October 2025 and February 2026 | look up the current ids at https://atlas.mitre.org/ |
| `{{RESIDUAL_*}}` | per research-synthesis residual risks and any the architect surfaces | each row has an expiration so the risk cannot sit forever |
| `{{ARCHITECT_VERSION}}` | git SHA of `skills/slo-architect/SKILL.md` at emit time | provenance |
| `{{#IF compliance_includes_gdpr}}...{{/IF}}`, `{{#IF ai_component}}...{{/IF}}` | conditional blocks | emit only when condition is true; never empty stubs |

## Required sections (structural-contract test asserts presence)

- `STRIDE per component` — table with all six STRIDE classes as headers.
- `Abuse cases` — table with the six columns listed above.
- `Compliance mapping` — default SOC 2 + OWASP ASVS 5.0 columns.
- `AI / LLM-specific threats` — conditional; cites MITRE ATLAS + OWASP LLM Top 10 + NIST AI RMF.
- `Residual risks` — table with Risk / Exploit path / Compensating / Owner / Review-by columns.
- `Provenance` — links back to the inputs that produced the file.

## Anti-patterns

- **Filling every STRIDE cell for every component**: the value is in naming classes that *do* apply, not in enumerating every combination. `N/A — <reason>` is valid for cells with no surface.
- **Leaving `AI / LLM-specific threats` populated when `ai_component: false`**: the conditional block is the gate. An AI-triad section on a non-AI target is noise that dilutes the real signal.
- **Free-Markdown interpolation of user-provided text**: every `{{USER_PROVIDED_*}}` token renders inside `~~~text`. Deviating from this rule is the vector for template-placeholder injection.
- **Bug-instance framing**: cells say "eliminated by &lt;control&gt;" / "mitigated by &lt;control&gt;", not "SQLi might be here." Class-elimination framing is what `/slo-critique` downstream relies on.

## SLO JSON companion serialization mapping

`/slo-architect` Step 3.5 ALSO emits `docs/slo/design/<slug>-threat-model.slo.json`, the machine-readable companion conforming to [`references/security/threat-model-schema.md`](../../../references/security/threat-model-schema.md). This section is the Markdown→JSON mapping. The JSON is the source of truth read by `/slo-critique` and `/slo-verify`; the Markdown remains the human-reviewed artifact. Both carry the same content.

| Markdown source | JSON field | Rule |
|---|---|---|
| `STRIDE per component` cell | `stride[]` `{component, class, state, control_or_reason}` | `state` ∈ `eliminated\|mitigated\|na\|residual` |
| Abuse-case row id `tm-<slug>-abuse-N` | `abuse_cases[].id` | **1:1, frozen. The id maps onto the JSON `id` field unchanged. Renumber is forbidden** — a changed abuse case is `status: superseded` + non-empty `superseded_by` + `supersede_reason`, never renumbered, never silently dropped (supersede-don't-renumber). |
| Abuse-case attacker / step / outcome / control | `abuse_cases[]` string fields | descriptive strings only |
| `Residual risks` row | `residual_risks[]` incl. `accepted_residual` | `accepted_residual: true` ⇒ knowingly accepted, NOT missing coverage |
| `Compliance mapping` row | `compliance[]` | SOC 2 + ASVS default columns |
| Provenance block | `provenance` | `producer_skill_sha` = git SHA of `skills/slo-architect/SKILL.md` at emit; `inputs[].sha` = git blob SHAs (idiom of `references/sast/manifest-schema.md`; not a content hash) |
| (per entry) sensitivity | `sensitivity` / per-entry `classification` | `public\|internal\|confidential\|restricted` |

**SEC-1 producer-side neutralisation.** Emit the JSON via a structural JSON serializer (or escape equivalently). User-controlled idea-doc / Top-risks text is serialized ONLY into descriptive string fields and **never** chooses `id`, `classification`, `accepted_residual`, or `status` (author-controlled). This is the write-side analogue of the consumer `~~~text` fence rule; without it a crafted Top-risks string yields a schema-valid but semantically-poisoned `.slo.json`.
